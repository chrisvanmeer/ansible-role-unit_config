---
- name: Test role
  hosts: localhost
  become: true

  roles:
    - role: ansible-role-unit_config
      vars:
        unit_config_enabled: true
        unit_config:
          - name: consul
            Unit:
              Description: Consul is so much fun
